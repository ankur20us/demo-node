var add = require("../additon");
var addWithCallback = require("../additon/addwithcallback");
var sub = require("../substraction");
var div = require("../division");
var mul = require("../multiplication");

module.exports = {
    add : add,
    addWithCallback : addWithCallback,
    sub : sub,
    div : div,
    mul : mul
};
